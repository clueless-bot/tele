-- The watch_history table is created by 0037_add_watch_history.sql.
-- This generated migration intentionally remains a no-op so existing databases
-- and fresh databases both record the Drizzle schema snapshot without trying
-- to create the table twice.
SELECT 1;
